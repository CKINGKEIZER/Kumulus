# KpiTable
Compact label→value(s) KPI block (lighter than FinancialTable). Operational metrics, unit economics.

```jsx
<KpiTable rows={[{label:"Recurring revenue %",values:["78%"]},{label:"Customers",values:["450+"]}]} />
```
For full financial statements use FinancialTable instead.