import * as React from "react";
export interface KeyMessageBandProps {
  children: React.ReactNode;
  className?: string;
  style?: React.CSSProperties;
}
/** One-line serif takeaway strip under the slide title. */
export function KeyMessageBand(props: KeyMessageBandProps): JSX.Element;
