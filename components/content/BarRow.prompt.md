# BarRow
Horizontal bar with label + value capsule — top-N lists, ranked comparisons.

```jsx
<BarRow label="Customer #1" value={1.0} max={1.0} capsule="€1.0m" />
```
Navy bar + pale value capsule; set `max` across a group so bars share a scale.