import * as React from "react";
export interface CheckPillHeaderProps { items: React.ReactNode[]; className?: string; style?: React.CSSProperties; }
export function CheckPillHeader(props: CheckPillHeaderProps): JSX.Element;
