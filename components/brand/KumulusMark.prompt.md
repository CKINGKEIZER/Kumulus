# KumulusMark
Permanent Kumulus **advisor** wordmark (navy + coral dot). Footer bottom-right of every content slide and cover; never swapped per deal.

```jsx
<KumulusMark height={40} />
```
Props: `height` (px). Never use for the target company — that's TargetLogo.