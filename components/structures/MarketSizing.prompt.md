# MarketSizing (skeleton)
STR-MARKET-SIZING — a sizing device (Funnel / Pyramid / stacked ColumnChart) + grounded commentary. Inside `<SlideFrame variant="content">`.

```jsx
<MarketSizing
  device={<Funnel stages={[{label:"TAM",value:"€2.4bn"},{label:"SAM",value:"€480m"},{label:"SOM",value:"€120m"}]} />}
  commentary={["EU explosion-safety market growing at 5.8% CAGR (2018–2025)","Irmaco targets ~10% of the addressable segment"]} />
```
