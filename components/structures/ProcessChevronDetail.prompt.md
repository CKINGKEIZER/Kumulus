# ProcessChevronDetail (skeleton)
STR-PROCESS-CHEVRON-DETAIL — chevron step headers + a detail column of bullets per step. Inside `<SlideFrame variant="content">`.

```jsx
<ProcessChevronDetail steps={[{label:"Phase 1"},{label:"Phase 2"},{label:"Phase 3"}]}
  details={[["Assess","Scope"],["Integrate","Migrate"],["Optimise","Scale"]]} />
```
