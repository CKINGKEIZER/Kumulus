# ValueChain (skeleton)
STR-VALUE-CHAIN — a value-chain chevron band + insight commentary. Inside `<SlideFrame variant="content">`.

```jsx
<ValueChain stages={[{label:"R&D"},{label:"Manufacturing"},{label:"Distribution"},{label:"Service"}]}
  insight={["Irmaco owns distribution + service; manufacturing largely third-party"]} />
```
