# CustomerDeepDive (skeleton)
STR-CUSTOMER-DEEPDIVE — 2-3 customer chart panels + optional client table. Inside `<SlideFrame variant="content">`.

```jsx
<CustomerDeepDive panels={[<ColumnChart .../>, <ColumnChart mode="stacked" .../>]} clients={<ComparisonMatrix .../>} />
```
