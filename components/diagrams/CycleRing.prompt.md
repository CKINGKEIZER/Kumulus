# CycleRing
Repeating growth motion (flywheel / cycle) — N stage cards around a dashed ring with an optional centre label.

```jsx
<CycleRing centerLabel="Compounding flywheel"
  stages={["Win new installs","Grow installed base","Recurring service","Reinvest in R&D"]} />
```
