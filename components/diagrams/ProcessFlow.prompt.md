# ProcessFlow
Chevron process / value-chain row (LP-PROCESS-CHEVRON). Steps in deal primary, optional body beneath each.

```jsx
<ProcessFlow ramp steps={[{label:"Source",body:"..."},{label:"Distribute"},{label:"Service"}]} />
```
ramp = tint light→deep across steps.